export interface daysToGenerateT{
    date : string,
    day:string,
    daysToGenerate:number,
    dayOfMonth:number,
    month:string,
}

export interface hoursToGenerateT{
    startHour:number,
    endHour:number,
    interval:number,
    hour:number,
    minute:number,
}

export interface yearsToGenerateT{
    startYear:number,
    endYear:number,
    year:number,
}

